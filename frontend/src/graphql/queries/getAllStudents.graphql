query GetAllStudents($limit: Int!, $offset: Int!) {
    students(take: $limit, skip: $offset, orderBy: {lastName: asc}) {
        __typename
        id
        name
        firstName
        lastName
        portalId
    }
}