mutation UpdateStudentInfo($id: ID!, $firstName: String!, $lastName: String!, $isFormer: Boolean!) {
  updateStudent(where: {id: $id}, data: {firstName: $firstName, lastName: $lastName, isFormer: $isFormer}){
    __typename
    id
  }
}